// Interfaces
import { FC } from "react";
import { EntityContainerInterfaceProps } from "@/core/interfaces";

const TagInterface: FC<EntityContainerInterfaceProps> = ({ containerType }) => {
  return <div>TagInterface</div>;
};

export default TagInterface;
